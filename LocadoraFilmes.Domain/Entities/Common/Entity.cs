namespace LocadoraFilmes.Domain.Entities.Common
{
    // Esta classe serve como base para todas as entidades no domínio.
    // Ela foi criada para permitir a extensão por outras entidades,
    // garantindo que o BaseRepository só possa ser acionado quando a TEntity
    // for uma extensão direta desta classe.
    public class Entity
    {
    }
}
